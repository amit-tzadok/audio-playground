FROM python:3.11-slim
WORKDIR /app
RUN apt-get update && apt-get install -y --no-install-recommends \
    git git-lfs libsndfile1 ffmpeg build-essential \
    && rm -rf /var/lib/apt/lists/*
RUN pip install --no-cache-dir torch==2.1.1 torchaudio==2.1.1 --index-url https://download.pytorch.org/whl/cpu
RUN pip install --no-cache-dir resemble-enhance fastapi "uvicorn[standard]" python-multipart
COPY app /app/app
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8100"]
