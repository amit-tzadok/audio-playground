apiVersion: v1
kind: Secret
metadata:
  name: hf-token
  namespace: audio-platform
type: Opaque
stringData:
  # Hugging Face access token with permission to use pyannote/speaker-diarization-3.1.
  HF_TOKEN: "REPLACE_ME"
